# Implementation TODO: Agent I/O-First Learning Agent

Goal: make Pi the central tutor-agent by giving it a small, powerful set of deterministic tools and compact context packets. Leave UI/frontend for later.

## Phase 0 — Stabilize current bricks

- [ ] Confirm current JSON schemas are acceptable:
  - KG graph JSON.
  - Learner profile JSON.
  - Parser chunk JSONL.
- [ ] Add or standardize node metadata fields:
  - `extended_description`
  - `difficulty`
  - `mastery_threshold`
  - `estimated_minutes`
  - `exam_relevance`
  - `topic`
  - `is_threshold_concept`
  - `common_traps`
  - `exercise_ladder_level`
- [ ] Add schema/version notes in docs for each brick.
- [ ] Keep the architecture invariant explicit:
  - one canonical teacher KG;
  - one learner overlay keyed by `KnowledgeNode.id`;
  - learner KG is a dynamic view, not a duplicated graph.

## Phase 1 — KG query/tool layer

Implement a new `tools` or `agent_io` brick exposing stable function-like calls for Pi.

- [ ] Implement `get_node(query_or_id, fields=None, include_relatives=False, include_sources=True, include_user_state=True)`.
  - Accept node id, exact label, alias, or fuzzy concept string.
  - Return compact node packet.
- [ ] Implement `get_node_relatives(node, depth=1, direction="both", relation_types=None, unpack=False)`.
  - `parents` = prerequisites.
  - `children` = dependents.
  - `both` = both directions.
- [ ] Implement `get_from_first_principles(node, max_paths=3, pruned=True)`.
  - Return prerequisite chains from foundations to target.
- [ ] Implement graph search over current KG:
  - labels;
  - aliases;
  - descriptions;
  - formulas;
  - source refs/quotes.
- [ ] Add tests for all KG query calls.
- [ ] Add CLI wrappers for the calls so Pi can use them through shell if needed.

## Phase 2 — Context retrieval

Build the main retrieval abstraction Pi should use.

- [ ] Implement `context_retrieve(query, intent="teach", limit=8, include_user_state=True)`.
- [ ] Retrieval should initially use:
  - exact id/label lookup;
  - alias lookup;
  - lexical/fuzzy search;
  - formula string matching;
  - source quote/chunk search;
  - KG neighbor expansion;
  - learner overlay attachment.
- [ ] Return a compact packet:
  - matched nodes;
  - formulas;
  - prerequisites;
  - related problem types;
  - source quotes;
  - learner state;
  - retrieval confidence/scores.
- [ ] Keep vector DB optional behind this interface.
- [ ] Add tests with tiny demo KG and learner profile.

## Phase 3 — ZPD and learning path command hardening

The learner brick already has first versions. Make them Pi-facing and stable.

- [ ] Finalize `get_proximal_dev(max_results=10, mode="exam_sprint")` output shape.
- [ ] Include in each ZPD candidate:
  - node id;
  - label;
  - mastery;
  - threshold;
  - difficulty;
  - exam relevance;
  - blocking prerequisites;
  - short deterministic reason.
- [ ] Finalize `get_learning_path(goal_node=None, mode="exam_sprint", max_steps=12)`.
- [ ] If no goal node is provided, generate a path from high-value ZPD nodes.
- [ ] Add path/session grouping for short ADHD-friendly sessions.
- [ ] Add tests for blocked, ready, mastered, and no-goal cases.

## Phase 4 — Informal learner memory

Separate formal mastery from informal user adaptation.

- [ ] Implement `learner_memory.json` model.
- [ ] Implement `get_user_memory()`.
- [ ] Implement `update_user_memory(memory_patch)`.
- [ ] Track:
  - learning style;
  - explanation preferences;
  - burnout/focus state;
  - recurring difficulties;
  - exam constraints;
  - recent session summaries.
- [ ] Keep memory concise and editable.
- [ ] Add tests for patch/update/roundtrip.

## Phase 5 — Attempt logging and deterministic learner updates

Separate evidence recording from mastery updates.

- [ ] Implement append-only `attempts.jsonl`.
- [ ] Implement `record_attempt(attempt_json) -> attempt_id`.
- [ ] Attempt fields:
  - exercise id if present;
  - raw answer text;
  - timestamp generated by system;
  - time spent;
  - confidence;
  - session id;
  - scratch notes.
- [ ] Implement `update_learner(assessment_json) -> mastery update packet` on top of existing BKT code.
- [ ] Pi should emit node evaluations, not direct mastery values.
- [ ] Assessment fields:
  - node id;
  - correct/partial correctness;
  - quality 0-5;
  - mistake text;
  - confidence;
  - question type;
  - evidence quote from attempt.
- [ ] Ensure each update records deterministic UTC timestamps.
- [ ] Ensure each update can update spaced-repetition fields.
- [ ] Add tests for BKT update packets and attempt logs.

## Phase 6 — Exercise indexing brick

Essential for Physics exam training.

- [ ] Define `Exercise` model:
  - `exercise_id`
  - source path/document/page if available;
  - statement;
  - required node ids;
  - problem type;
  - difficulty;
  - number of steps;
  - algebra load;
  - trap flags;
  - solution availability;
  - solution text/source if available.
- [ ] Build initial JSONL exercise store.
- [ ] Implement manual/demo exercise examples first.
- [ ] Implement `search_exercises(query=None, target_nodes=None, difficulty=None, max_new_concepts=1, source_priority="course", limit=5)`.
- [ ] Implement `get_exercise(exercise_id, include_solution=False, include_context=True)`.
- [ ] Later: parse exercises from course PDFs and solution PDFs.
- [ ] Add tests for exercise search by node, difficulty, and ladder level.

## Phase 7 — Teaching context packer

This is the main tool Pi should call before teaching.

- [ ] Implement `pack_teaching_context(mode, exercise_id=None, target_node=None, query=None, token_budget=3000)`.
- [ ] Packet should include:
  - current objective;
  - selected exercise if any;
  - relevant KG nodes;
  - formulas;
  - prerequisites;
  - source quotes;
  - learner state for relevant nodes;
  - ZPD snapshot;
  - recent mistakes;
  - informal user memory;
  - allowed actions.
- [ ] Implement token/size budget trimming.
- [ ] Add mode presets:
  - `guided_exercise`
  - `concept_repair`
  - `exam_sprint`
  - `review`
  - `burnout_mode`
- [ ] Add tests that packet is compact and contains required fields.

## Phase 8 — Optional vector index

Add only after lexical/KG retrieval works.

- [ ] Decide vector backend:
  - likely `sqlite-vec` for one local DB;
  - Chroma or LanceDB only if simpler in practice.
- [ ] Decide embedding model:
  - small multilingual embedding model for Italian/English Physics;
  - keep embeddings replaceable.
- [ ] Implement vector indexing for:
  - node labels/descriptions;
  - extended descriptions;
  - source chunks;
  - exercises.
- [ ] Integrate vector results behind `context_retrieve`.
- [ ] Do not expose vector DB directly to Pi.
- [ ] Add tests that retrieval works with vector backend disabled.

## Phase 9 — Pi integration layer

Expose the tool calls in the way Pi can actually use them.

- [ ] Choose integration shape:
  - CLI commands first;
  - Pi extension/custom tool later.
- [ ] Provide a single command namespace, e.g. `python -m ai_learning_agent.agent_io.cli ...`.
- [ ] Add commands:
  - `retrieve`
  - `node`
  - `relatives`
  - `first-principles`
  - `zpd`
  - `path`
  - `exercise-search`
  - `exercise-get`
  - `record-attempt`
  - `update-learner`
  - `memory-get`
  - `memory-update`
  - `pack-context`
- [ ] Add examples in README.
- [ ] Later: wrap these as first-class Pi tools.

## Phase 10 — End-to-end terminal tutoring loop

Before UI, prove the loop from terminal.

- [ ] Start session command:
  - loads KG;
  - loads learner profile;
  - loads memory;
  - gets ZPD/path;
  - picks exercise or concept.
- [ ] Generate context packet for Pi.
- [ ] Pi teaches/interacts.
- [ ] Record attempt.
- [ ] Pi emits assessment JSON.
- [ ] Deterministic update applies mastery changes.
- [ ] Memory gets concise session update.
- [ ] Repeat.

## Deferred: UI/frontend

Leave until the terminal/tool loop is reliable.

Future UI should visualize:

- current exercise;
- tutor chat;
- scratchpad;
- compact mastery/mirror KG;
- ZPD/next step;
- burnout-friendly session controls.
